"use strict";
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
Object.defineProperty(exports, "__esModule", { value: true });
exports.getRootProjectTypeNode = getRootProjectTypeNode;
exports.getTdpProjectTypeNode = getTdpProjectTypeNode;
const tslib_1 = require("tslib");
const teamsfx_api_1 = require("@microsoft/teamsfx-api");
const fs_extra_1 = tslib_1.__importDefault(require("fs-extra"));
const os_1 = tslib_1.__importDefault(require("os"));
const path_1 = tslib_1.__importDefault(require("path"));
const featureFlags_1 = require("../../../common/featureFlags");
const globalVars_1 = require("../../../common/globalVars");
const templateHelper = tslib_1.__importStar(require("../../../component/generator/templateHelper"));
const folder = tslib_1.__importStar(require("../../../folder"));
const constructNode_1 = require("../constructNode");
/**
 * Load the wizard question tree from wizardNode.json.
 * Combined JSON with all sub-trees inlined.
 */
function getRootProjectTypeNode(platform = teamsfx_api_1.Platform.VSCode) {
    return loadUiNode("wizardNode.json", platform);
}
/**
 * Load the TDP wizard question tree from tdpNode.json.
 * Subset of wizard options for Teams Developer Portal import flow.
 */
function getTdpProjectTypeNode(platform = teamsfx_api_1.Platform.VSCode) {
    return loadUiNode("tdpNode.json", platform);
}
function loadUiNode(fileName, platform) {
    const cachedJsonPath = path_1.default.join(os_1.default.homedir(), `.${String(teamsfx_api_1.ConfigFolderName)}`, "ui", fileName);
    // `constructNode` rebuilds this tree against THIS fx-core's `QuestionNames`,
    // option ids, and `onDidSelection` callbacks, and the v4 front-door adapter
    // (`applyV3PreFill`) pre-fills the v3 inputs against those same local names
    // (e.g. `inputs[QuestionNames.ActionType]`). A downloaded `~/.fx/ui` copy can
    // lag the local code (a dev build ahead of the published v4 metadata, or a
    // leftover cache from an earlier version); when its action-type node name
    // drifts from the pre-fill key the already-answered question is no longer
    // suppressed and is asked a second time. On the v4 channel, pin the UI tree to
    // the bundled artifact that ships with this fx-core so the v3 walk always
    // matches the adapter. (Metadata descriptors keep the `useBundledMetadataForV4`
    // escape hatch in metadata/index.ts — they carry no local code bindings.)
    const v4Enabled = featureFlags_1.featureFlagManager.getBooleanValue(featureFlags_1.FeatureFlags.V4Enabled);
    let jsonPath;
    let source;
    if (!v4Enabled && !templateHelper.useLocalTemplate() && fs_extra_1.default.pathExistsSync(cachedJsonPath)) {
        jsonPath = cachedJsonPath;
        source = "cache";
    }
    else {
        jsonPath = path_1.default.join(folder.getTemplatesFolder(), "ui", fileName);
        source = "bundled";
    }
    const content = fs_extra_1.default.readFileSync(jsonPath, "utf-8");
    globalVars_1.TOOLS?.logProvider?.info(`[Dynamic Template] Loaded ${fileName} from ${source}: ${jsonPath}`);
    return (0, constructNode_1.constructNode)(content, platform);
}
//# sourceMappingURL=rootNode.js.map