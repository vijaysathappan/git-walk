"use strict";
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
Object.defineProperty(exports, "__esModule", { value: true });
exports.scaffoldV4Deps = void 0;
exports.scaffoldV4 = scaffoldV4;
exports.collectCreateFloor = collectCreateFloor;
const tslib_1 = require("tslib");
/**
 * The composition-root impl of the flag-on `createProjectFrontDoor` seams
 * (`dispatch-create-by-engine` `CreateFrontDoorDeps`). These live outside the
 * pure orchestrator so it stays injectable and I/O-free: `FxCore` wires these
 * real handlers, the orchestrator's tests wire fakes.
 *
 * - `scaffoldV4`     — the `engine: "v4"` hand-off: build a v3 `GeneratorContext`
 *                      over the create floor and render the authored declarative
 *                      package through the v4 distribution channel.
 */
const teamsfx_api_1 = require("@microsoft/teamsfx-api");
const fs = tslib_1.__importStar(require("fs-extra"));
const jsonschema = tslib_1.__importStar(require("jsonschema"));
const path_1 = tslib_1.__importDefault(require("path"));
const telemetry_1 = require("../common/telemetry");
const globalVars_1 = require("../common/globalVars");
const coordinator_1 = require("../component/coordinator");
const generator_1 = require("../component/generator/generator");
const utils_1 = require("../component/generator/utils");
const v4TemplateBridge_1 = require("../component/generator/v4TemplateBridge");
const telemetry_2 = require("../component/telemetry");
const ManifestUtils_1 = require("../component/driver/teamsApp/utils/ManifestUtils");
const pathUtils_1 = require("../component/utils/pathUtils");
const common_1 = require("../error/common");
const question_1 = require("../question");
const v4_1 = require("../v4");
const runtimeRegistry_1 = require("../v4/runtime/runtimeRegistry");
/** The package namespace the create front door opens v4 packages under. */
const CREATE_KIND = "create";
/** The language a single-language (language-neutral) v4 package scaffolds under. */
const COMMON_LANGUAGE = "common";
function scaffoldTelemetryProps(inputs, target, language) {
    const templateName = inputs[question_1.QuestionNames.TemplateName];
    const templateId = typeof templateName === "string" && templateName.length > 0 ? templateName : target.templateId;
    return {
        [telemetry_1.TelemetryProperty.Component]: telemetry_1.Component.core,
        [telemetry_1.TelemetryProperty.TemplateName]: `${templateId}-${(0, utils_1.convertToLangKey)(language)}`,
        env: process.env.TEAMSFX_ENV || "",
    };
}
/**
 * The one module function `scaffoldV4` hands the located package to, behind the
 * repo's `*Deps` seam so a test can stub the channel render without I/O (the v4
 * named export is otherwise a read-only binding).
 */
exports.scaffoldV4Deps = {
    scaffoldDeclarativeFromV4Channel: v4TemplateBridge_1.scaffoldDeclarativeFromV4Channel,
};
/**
 * The `engine: "v4"` hand-off. The orchestrator has already collected the
 * package's own answers (Q2, via `runCreateInputs`); this validates the create
 * floor (`folder` / `app-name`), then renders the located `create/<templateId>`
 * declarative package onto disk via the v4 distribution channel.
 *
 * Mirrors the legacy customized-generator validation, tracking-id and manifest
 * short-name-trim tail so a v4 scaffold yields the same `CreateProjectResult`
 * shape as every other create path.
 */
async function scaffoldV4(inputs, target, answers, flagReader, resolvedPackage) {
    const folderInput = inputs[question_1.QuestionNames.Folder];
    if (!folderInput) {
        return (0, teamsfx_api_1.err)(new common_1.MissingRequiredInputError(question_1.QuestionNames.Folder));
    }
    const folder = path_1.default.resolve(folderInput);
    const appName = inputs[question_1.QuestionNames.AppName];
    if (appName === undefined) {
        return (0, teamsfx_api_1.err)(new common_1.MissingRequiredInputError(question_1.QuestionNames.AppName));
    }
    const validateResult = jsonschema.validate(appName, { pattern: question_1.AppNamePattern });
    if (validateResult.errors && validateResult.errors.length > 0) {
        return (0, teamsfx_api_1.err)(new common_1.InputValidationError(question_1.QuestionNames.AppName, validateResult.errors[0].message));
    }
    const projectPath = path_1.default.join(folder, appName);
    // The language axis is the downstream `collect-inputs` Q0 answer (ADR-0014
    // Amendment 2 / ADR-0016 decision 5); a single-language (`["common"]`) template
    // never asks it, so an absent answer falls back to the language-neutral floor.
    const languageAnswer = answers["language"];
    const language = typeof languageAnswer === "string" ? languageAnswer : COMMON_LANGUAGE;
    const telemetryProps = scaffoldTelemetryProps(inputs, target, language);
    const generatorContext = {
        name: appName,
        language,
        platform: inputs.platform,
        destination: projectPath,
        logProvider: globalVars_1.TOOLS.logProvider,
        onActionError: generator_1.templateDefaultOnActionError,
    };
    const locator = { kind: CREATE_KIND, templateId: target.templateId };
    const callerFloor = { appName, language };
    try {
        const source = await exports.scaffoldV4Deps.scaffoldDeclarativeFromV4Channel(generatorContext, locator, answers, callerFloor, telemetryProps, flagReader, resolvedPackage, (0, runtimeRegistry_1.createStepRegistry)((0, v4_1.createGeneralSensitivityLabelService)(globalVars_1.TOOLS.tokenProvider.m365TokenProvider)));
        if (source.warning) {
            globalVars_1.TOOLS.logProvider.warning(source.warning);
        }
    }
    catch (e) {
        const fxError = (0, common_1.assembleError)(e);
        (0, telemetry_2.sendErrorEvent)(telemetry_1.TelemetryEvent.GenerateTemplate, fxError, telemetryProps);
        return (0, teamsfx_api_1.err)(fxError);
    }
    (0, telemetry_2.sendSuccessEvent)(telemetry_1.TelemetryEvent.GenerateTemplate, telemetryProps);
    const result = { projectPath };
    // The scaffolding summary and the surfaces' post-create notifications read these; dropping
    // them here is what would make a placeholder-bearing m365agents.yml fail silently later.
    if (generatorContext.warnings?.length) {
        result.warnings = generatorContext.warnings;
    }
    const ymlPath = pathUtils_1.pathUtils.getYmlFilePath(projectPath, "dev");
    if (ymlPath && (await fs.pathExists(ymlPath))) {
        const ensureRes = await coordinator_1.coordinator.ensureTrackingId(projectPath, inputs.projectId);
        if (ensureRes.isErr()) {
            return (0, teamsfx_api_1.err)(ensureRes.error);
        }
        result.projectId = ensureRes.value;
    }
    const trimRes = await ManifestUtils_1.manifestUtils.trimManifestShortName(projectPath);
    if (trimRes.isErr()) {
        return (0, teamsfx_api_1.err)(trimRes.error);
    }
    return (0, teamsfx_api_1.ok)(result);
}
function getStringValidationFunc(validation) {
    if (validation === undefined || !("validFunc" in validation)) {
        return undefined;
    }
    return validation.validFunc;
}
async function resolveStringValue(value, inputs) {
    return typeof value === "function" ? await value(inputs) : value;
}
async function validateAppNameInput(inputs, appName) {
    const validation = (0, question_1.appNameQuestion)().validation;
    const validFunc = getStringValidationFunc(validation);
    if (validFunc !== undefined) {
        const validationMessage = await validFunc(appName, inputs);
        if (validationMessage !== undefined) {
            return (0, teamsfx_api_1.err)(new common_1.InputValidationError(question_1.QuestionNames.AppName, validationMessage, "createFrontDoor"));
        }
    }
    return (0, teamsfx_api_1.ok)(undefined);
}
/**
 * The `engine: "v4"` create-floor collection. The front door owns Q1/Q2, so the
 * remaining surface floor is collected directly here instead of routing through
 * any legacy question-tree traversal.
 */
async function collectCreateFloor(inputs, ui) {
    const folder = (0, question_1.folderQuestion)();
    const appName = (0, question_1.appNameQuestion)();
    if (inputs[question_1.QuestionNames.Folder] === undefined) {
        const defaultFolder = await resolveStringValue(folder.default, inputs);
        if (inputs.nonInteractive) {
            if (defaultFolder !== undefined) {
                inputs[question_1.QuestionNames.Folder] = defaultFolder;
            }
        }
        else {
            const folderResult = await ui.selectFolder({
                name: folder.name,
                title: (await resolveStringValue(folder.title, inputs)) ?? "",
                placeholder: await resolveStringValue(folder.placeholder, inputs),
                prompt: await resolveStringValue(folder.prompt, inputs),
                default: defaultFolder,
                validation: getStringValidationFunc(folder.validation),
            });
            if (folderResult.isErr()) {
                return (0, teamsfx_api_1.err)(folderResult.error);
            }
            if (typeof folderResult.value.result === "string") {
                inputs[question_1.QuestionNames.Folder] = folderResult.value.result;
            }
        }
    }
    const existingAppName = inputs[question_1.QuestionNames.AppName];
    if (typeof existingAppName === "string") {
        return validateAppNameInput(inputs, existingAppName);
    }
    const defaultAppName = await resolveStringValue(appName.default, inputs);
    if (inputs.nonInteractive) {
        if (defaultAppName === undefined) {
            return (0, teamsfx_api_1.err)(new common_1.MissingRequiredInputError(question_1.QuestionNames.AppName, "createFrontDoor"));
        }
        inputs[question_1.QuestionNames.AppName] = defaultAppName;
        return validateAppNameInput(inputs, defaultAppName);
    }
    const appNameResult = await ui.inputText({
        name: appName.name,
        title: (await resolveStringValue(appName.title, inputs)) ?? "",
        placeholder: await resolveStringValue(appName.placeholder, inputs),
        prompt: await resolveStringValue(appName.prompt, inputs),
        default: defaultAppName,
        validation: getStringValidationFunc(appName.validation),
    });
    if (appNameResult.isErr()) {
        return (0, teamsfx_api_1.err)(appNameResult.error);
    }
    if (typeof appNameResult.value.result === "string") {
        inputs[question_1.QuestionNames.AppName] = appNameResult.value.result;
    }
    return (0, teamsfx_api_1.ok)(undefined);
}
//# sourceMappingURL=createFrontDoorAdapters.js.map