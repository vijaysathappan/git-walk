import { Platform } from "@microsoft/teamsfx-api";
import { Template } from "./interface";
export declare function getAllTemplatesOnPlatform(platform: Platform): Template[];
export interface TemplateGroup {
    name: string;
    alias?: string;
    displayName: string;
    description: string;
    language: string;
}
/**
 * Group templates by name, ignoring programming language. The first language
 * encountered for a given name wins, and the display name falls back to
 * `displayName || alias || name`.
 */
export declare function groupTemplatesByName(templates: Template[]): TemplateGroup[];
export declare function listAllTemplates(): TemplateGroup[];
export declare function listDeclarativeAgentTemplates(): TemplateGroup[];
export declare function getDefaultTemplatesOnPlatform(platform: Platform): Template[];
//# sourceMappingURL=index.d.ts.map