"use strict";
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
Object.defineProperty(exports, "__esModule", { value: true });
exports.scaffoldFromPackageDir = scaffoldFromPackageDir;
const teamsfx_api_1 = require("@microsoft/teamsfx-api");
const neverthrow_1 = require("neverthrow");
const packageDir_1 = require("../distribution/packageDir");
const realRuntime_1 = require("./realRuntime");
const scaffold_1 = require("./scaffold");
const SOURCE = "Scaffold";
function scaffoldFrontDoorError(error) {
    if (error instanceof teamsfx_api_1.SystemError) {
        return error;
    }
    return new teamsfx_api_1.SystemError({
        source: SOURCE,
        name: "ScaffoldFromPackageDirFailed",
        message: "Failed to scaffold from the template package directory.",
    });
}
/** Product front door for scaffolding an on-disk declarative package. */
async function scaffoldFromPackageDir(packageDir, answers, callerFloor, targetDir, flagReader) {
    const loaded = (0, packageDir_1.loadPackageDir)(packageDir);
    if (loaded.isErr()) {
        return (0, neverthrow_1.err)(loaded.error);
    }
    try {
        const runtime = (0, realRuntime_1.createRealRuntime)(targetDir.path, flagReader);
        return await (0, scaffold_1.scaffold)({
            descriptor: loaded.value.descriptor,
            pipeline: loaded.value.pipeline,
            content: loaded.value.content,
            answers,
            callerFloor,
            targetDir,
        }, runtime);
    }
    catch (error) {
        return (0, neverthrow_1.err)(scaffoldFrontDoorError(error));
    }
}
//# sourceMappingURL=scaffoldFromPackageDir.js.map