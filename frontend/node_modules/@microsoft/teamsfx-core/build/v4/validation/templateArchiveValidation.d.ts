import type { FxError } from "@microsoft/teamsfx-api";
import type { Result } from "neverthrow";
import type { LoadedPackage } from "../distribution/packageDir";
import type { DeclarativeLocator } from "../model/dataModel";
import type { ValidateMode } from "./validateTemplatePackage";
/** Adapt validation diagnoses to the concrete errors owned by the caller. */
export interface TemplateArchiveErrorFactory {
    user(name: string, message: string): FxError;
    system(name: string, message: string): FxError;
}
/** Validate and open one declarative package from final channel archive bytes. */
export declare function validateDeclarativePackageArchive(bytes: Buffer, locator: DeclarativeLocator, mode: ValidateMode, engineVersion: string, errors: TemplateArchiveErrorFactory): Result<LoadedPackage, FxError>;
/** Validate every authored create/modify package in final channel archive bytes. */
export declare function validateDeclarativeTemplateArchive(bytes: Buffer, mode: ValidateMode, engineVersion: string, errors: TemplateArchiveErrorFactory): Result<string[], FxError>;
//# sourceMappingURL=templateArchiveValidation.d.ts.map