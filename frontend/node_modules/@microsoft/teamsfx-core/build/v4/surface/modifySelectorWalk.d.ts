import { FxError, UserInteraction } from "@microsoft/teamsfx-api";
import { Result } from "neverthrow";
import { BuildTarget } from "../buildTarget/resolveBuildTarget";
export interface ModifySelectorDeps {
    flagReader?: (name: string) => boolean;
    selectorBytesKind?: "zip" | "json";
    v4Registry?: (templateId: string) => boolean;
    prefilled?: Record<string, string>;
    interactive?: boolean;
}
export declare function runModifySelector(floorBytes: Buffer, ui: UserInteraction, surface: string, deps?: ModifySelectorDeps): Promise<Result<BuildTarget, FxError>>;
//# sourceMappingURL=modifySelectorWalk.d.ts.map