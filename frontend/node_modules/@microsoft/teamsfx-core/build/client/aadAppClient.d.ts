import { LogProvider, M365TokenProvider } from "@microsoft/teamsfx-api";
import { AadOwner } from "../common/permissionInterface";
import { AADApplication } from "../component/driver/aad/interface/AADApplication";
import { AADManifest } from "../component/driver/aad/interface/AADManifest";
import { SignInAudience } from "../component/driver/aad/interface/signInAudience";
export declare class AadAppClient {
    private readonly retryNumber;
    private readonly tokenProvider;
    private readonly logProvider;
    private readonly axios;
    private readonly baseUrl;
    constructor(m365TokenProvider: M365TokenProvider, logProvider?: LogProvider);
    createAadApp(displayName: string, signInAudience?: SignInAudience, serviceManagementReference?: string, isMicrosoftUser?: boolean): Promise<AADApplication>;
    deleteAadApp(id: string): Promise<void>;
    generateClientSecret(objectId: string, clientSecretExpireDays?: number, // Recommended lifetime from Azure Portal
    clientSecretDescription?: string, isMicrosoftUser?: boolean): Promise<string>;
    updateAadApp(manifest: AADManifest | AADApplication): Promise<void>;
    getOwners(objectId: string): Promise<AadOwner[] | undefined>;
    addOwner(objectId: string, userObjectId: string): Promise<void>;
    private is404Error;
    private is400Error;
}
//# sourceMappingURL=aadAppClient.d.ts.map