fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pandas>=2.0.0
openpyxl>=3.1.0
python-multipart>=0.0.6
pydantic>=2.0.0
sqlalchemy>=2.0.0
alembic>=1.13.0   # not yet wired — see Phase 5 (database): no migration currently runs, DDL lives in app/store/schema.py
asyncpg>=0.29.0   # not yet wired — see Phase 5 (database): the app talks to SQLite via the stdlib sqlite3 module, not Postgres
redis>=5.0.0      # not yet wired — see Phase 5 (database): no cache/queue backend is in use yet
boto3>=1.34.0
paramiko>=3.4.0
zstandard>=0.22.0
